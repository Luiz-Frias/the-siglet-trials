# Data

This directory contains all data used in the project.

- `raw/`: Original, immutable data
- `processed/`: Cleaned, transformed data ready for modeling
- `interim/`: Intermediate data that has been transformed but not finalized
- `external/`: Data from external sources

All data is tracked with DVC (Data Version Control).
